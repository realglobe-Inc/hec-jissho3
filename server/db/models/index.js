const Camera = require('./camera_model')
const Photo = require('./photo_model')
const User = require('./user_model')
const Report = require('./report_model')
const ReportInfo = require('./report_info_model')

module.exports = {
  User,
  Camera,
  Photo,
  Report,
  ReportInfo
}
