### Setup

Install

```bash
$ git clone https://github.com/{{pkg.repository}}.git
$ cd {{pkg.name}}
$ npm install
```

In develepment

```bash
$ npm run dev
```

In production, compile and start the servers.

```bash
$ NODE_ENV=production npm start
```
