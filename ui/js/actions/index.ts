import * as callers from './callers'
import * as showPanel from './show_panel'
import * as map from './map'
import * as markers from './markers'
import * as modalWindow from './modal_window'
import * as reportClosed from './report_closed'
import * as reportShared from './report_shared'
import * as reports from './reports'
import * as selectedMarker from './selected_marker'
import * as photos from './photos'
import * as selectedPhoto from './selected_photo'

const actions = {
  callers,
  showPanel,
  map,
  markers,
  modalWindow,
  reportClosed,
  reportShared,
  reports,
  selectedMarker,
  photos,
  selectedPhoto
}

export default actions
